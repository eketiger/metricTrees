---
title: Authentication
description: Authenticate API requests with a bearer token.
---

Pass `Authorization: Bearer <YOUR_TOKEN>` on every request. Tokens are issued from
Settings → API keys. Keys are secret — treat them like passwords.
